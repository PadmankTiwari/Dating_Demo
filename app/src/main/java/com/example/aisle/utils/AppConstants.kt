package com.example.aisle.utils

object AppConstants {
    const val ISE_VALUE = "should never call auto-cleared-value get when it might not be available"

    //otp Fragment
    const val Max_Timer_Value = 60000
    const val Count_Donw_Interval = 1000
}